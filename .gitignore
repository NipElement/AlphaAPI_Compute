# Build outputs and caches — regenerated by `make web` / `make plugin`.
node_modules/
web/dist/
__pycache__/
*.pyc

# Local run bookkeeping (the evidence pack itself IS committed; this is the
# pointer to whichever run is current on this machine).
.run_id

# Editor / OS noise
.DS_Store
*.swp
