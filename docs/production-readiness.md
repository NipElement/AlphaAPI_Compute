# 生产就绪差距台账(Production Readiness Register)

> 生成:2026-08-19,六维深挖(30 agent、460 次工具核查,P0 逐条对抗复核)。
> 标尺:**公开、收费、跑在 4 台真 DGX B300 上**的 GPU 租赁服务;offer 页已承诺
> 整机 $52,750/月、按分钟 $9.57/卡·时、30TB 存储全含、NVLink 书面保证、双节点 XDR。
> 首要指令:**硬件到货后 setup→上线要最快**,一切可预建的现在就在 EC2 上预建并排练。

**总量:60 项差距 | P0=42 P1=17 P2=1 | 现在可建 40,需拍板 13,到货才做 7**
**2026-08-26 更新:WS1 完成后 — 已关闭 6 项(下表行内标〔DONE〕),部分完成 3 项(〔PARTIAL〕);开放:P0=37 P1=16 P2=0。**
**2026-08-27 22:00 更新:开放 P0=25(其中 PARTIAL 10) P1=8(PARTIAL 4) P2=0;已 DONE 27/60 项。开放项里有 14 项是 PARTIAL——剩余部分几乎全卡在 D1–D8。**

### 2026-08-26 状态更新(WS1 落地,含多智能体对抗审查修复)

- **dgx overlay 已含全部产品工作负载**(76 对象):gateway(SPA 经 arise/web 内容镜像 + initContainer,无 hostPath/docker-cp)、tenant-portal、ops-console、capacity-controller(VAST_ADAPTER=none 诚实无市场模式,ARISE↔DIRECT 全通,VAST 快速失败;继承态/记录态防护 + 隔离重申,tests/unit_adapter_modes.py 18 例)、monitoring(DCGM scrape 桩)、准入策略全集(owner-gate 含 DIRECT 分支、host-isolation、queue/priority/flavor/storage)、真存储(local-path@digest 于 /raid,`.raid-verified` 哨兵 fail-closed)。静态门 `make dgx-render`(scripts/dgx-render-check.sh,9 组断言:含全镜像 digest 固定或 day0-registry.invalid 哨兵、versions.env 交叉核对、DIRECT 分支存在性)。
- **base 泄漏已修**:vast-mock 家族 + fake-gpu RBAC 移入 overlays/lab/lab-simulation-identity.yaml(lab 渲染逐对象等价,独立复核)。
- **配额/限额已达硬件量级**:nvidia.com/gpu 24/16(此前真 GPU 无上限)、cpu 768/512、mem 6144/4096Gi、存储 30Ti、LimitRange max 整节点。
- **所有权交接新增卷门**:DIRECT 交接/VAST 上架/ARISE 回收均阻塞于节点残留租户 PV(StrandedVolumes 条件;controller RBAC +persistentvolumes 只读)。
- **make dgx-platform / dgx-code / dgx-deploy** 已存在(DGX_KCTX 参数化,零 kind-ism)。
- 仍开放(本节不改变):registry 本体、vendored Volcano、edge/TLS、verify-dgx、WS2-WS7 全部,及 D1-D8 决策。Day-0 步骤 6 的 `make deploy-dgx` 现为 `make dgx-deploy`;步骤 9 的存储类已预建(D2 仍开放数据路径承诺)。
- NetworkPolicy 的 kubelet 探针存活为 kindnetd 实测,**dgx CNI 选定后必须重验**(networkpolicies.yaml 内注记)。

### 2026-08-27 状态更新(WS2 落地:gateway 公网化)

已关闭 6 项、部分完成 1 项(行内标注);开放:P0=31 P1=15 P2=0。

- **凭据 fail-closed**:`GW_PUBLIC_MODE=true`(dgx 设置)下,种子密码未设/等于 README 默认值/短于 12 位,或 `GW_SESSION_KEY` 缺失/短于 32 位 → **进程拒绝启动**。凭据由 Secret `platform-gateway-auth` 注入(`make dgx-gateway-secret` 随机生成、只打印一次),清单里没有任何字面密码;render 门新增断言:四个凭据必须是 secretKeyRef,且全渲染中不得出现字面凭据 env。
- **会话无状态化**:HMAC 签名令牌(用户+身份版本+过期+jti)。发布不再把客户踢下线,第二副本在会话层已可行;种子账号的凭据材料由 SESSION_KEY 确定性派生,所以重启与多副本一致,而**轮换 Secret 里的密码会立即吊销该账号所有会话**。删号/改版本即时失效,登出记入有界撤销集。
- **登录限速**:源 IP 与用户名双键滑动窗口 + 锁定(默认 8 次/5 分钟 → 锁 15 分钟),判定在 PBKDF2 之前,锁定期间攻击者拿不到 CPU;返回 429 + Retry-After。
- **服务器硬化**:连接数上限(信号量,超限直接关闭而非排队)、每连接读超时、HTTP/1.1、daemon 线程;1 MiB 请求体上限保持。
- **CSRF / cookie**:跨站写按 Origin↔Host 拒绝;cookie 加 SameSite=Lax,公网模式加 `Secure` 与 `__Host-` 前缀 + HSTS(与 TLS 边缘一起打开,Day-0 步骤 12)。
- **访问日志**:每请求一行结构化日志(方法/路径/状态/客户端 IP/UA),健康探针与命中缓存的静态资源除外;`X-Forwarded-For` 仅在 `GW_TRUST_PROXY=true` 时采信(否则攻击者可伪造源 IP 绕过限速)。
- **锁定策略按键分离**(自审补强):IP 键严格(8 次/5 分钟 → 锁 15 分钟,只伤攻击者自己),用户名键宽松且短(20 次/15 分钟 → 锁 60 秒)。理由:任何知道账号名的人本可以用"账号锁定"把付费客户长期挡在门外——把我们的防御变成他的拒绝服务;用户名键的存在只为挡住分布式喷洒的 CPU 消耗,不该成为武器。
- **keep-alive 协议错位**(自审发现并修复):413 与 CSRF 拒绝在未读请求体时回包,HTTP/1.1 下未读字节会被当作下一个请求的开头——攻击者可控的请求体变成攻击者可控的请求。现在此类响应强制关闭连接。
- 测试:`tests/unit_gateway_security.py` 29 例(无集群,validate.sh §10);活集群实测登录/会话/代理越权/CSRF/限速/413/keep-alive 全部符合预期;UI-01、UI-02、UI-03 回归通过。
- 仍开放:TLS 边缘与域名本体(D4)、管理面与租户面同源分离(需拍板)、用户表持久化与 OIDC(D3)。

### 2026-08-27 状态更新(WS4 集群内半段 + WS7 补齐 + 边缘预建)

- **客户能 SSH 进自己的开发机了**。`services/devbox`:python 同 digest 基底 + openssh-server,sshd 以**非 root**(uid 65532)跑在 2222,在 restricted PSA 下(无 root、无 capability、只读 rootfs);公钥经 ConfigMap 注入(公钥不是秘密,无需扩大 Secret 权限),主机密钥启动时生成到 emptyDir;`internal-sftp` 让 scp/rsync/sftp 直接可用——**数据进出路径**同时解决。portal 对公钥做**数据级校验**(单行、已知类型、base64 体、拒绝 `command=` 等选项前缀——它最终进的是 sshd 解析的文件);删除级联 ConfigMap+Service。每台开发机 `/home/dev` 与 `/tmp` 可写(P1 "pip 都装不了"关闭)。本机 docker 探针 + ACC-01 矩阵用例验证:登录/可写/scp/密码拒绝/级联删除。**公网接入地址仍是 D4**;day-0 由运维 port-forward 或 bastion。
- **`infra/dgx/audit-policy.yaml`**:补上 kubeadm 配置引用但此前不存在的文件(API server 起不来的级别);11 条规则,所有权/节点/租户对象写入记完整请求体,Secret 只记元数据。
- **`infra/dgx/node-bootstrap.sh`**:每节点幂等引导(sysctl/inotify、swap、containerd SystemdCgroup、GPU 节点 RAID 哨兵**fail-closed**、头节点审计与备份目录、sshd 加固);kubeadm join **故意不内嵌 token**。
- **`infra/dgx/operators/`**:GPU/Network Operator values 草案。诚实之处:`driver.enabled` 与 OFED 归属**必须看到实机**(DGX OS 自带驱动,plan §13.2 禁止双管);织物 profile 依赖 D8/D5。已定的:不切分 GPU(整卡计费)、DCGM 开、DaemonSet 容忍全部 owner taint。
- **`platform/overlays/dgx/edge/`**:独立 kustomization——ingress-nginx(hostNetwork 头节点,digest 固定,webhook 不装)+ cert-manager values(三镜像 digest)+ Let's Encrypt ClusterIssuer + 网关 Ingress(限速)。**故意不在 `dgx-deploy` 里**:D4 未定,盲 apply 等于把网关暴露在头节点碰巧有的地址上。切流顺序写在头注。
- `runbooks/upgrade-rollback.md`:三层变更流程,层 0/2 已由本轮多次滚动与 MNT-01 演练。
- `docs/customer/quickstart.md`:客户文档,只写今天真实可用的路径,「即将」明确标注。

### 2026-08-27 状态更新(WS5 落地:计量骨架)

- **`services/metering`** —— 分配台账的唯一写入者。观察租户命名空间里持有 GPU/vCPU 的 Pod:开始运行时写 `open`,停止时写 `close`,**按 Pod UID 恰好一次**(重启后从台账尾重建 open 集,同 controller 的 transitionId 纪律)。`open.at` 取 API 记录的 `status.startTime`,`close.at` 优先取容器 `terminated.finishedAt` / `deletionTimestamp`,只有都没有时才用观测时间**并标明来源**——客户问起来时误差有界且可解释。
- **台账 = append-only JSONL + sha256 链**(每条记录 = sha256(前哈希 + 本记录)),启动时验链并导出 `arise_metering_ledger_chain_ok` 指标——坏账是**告警**,不是开票时的意外。单测证明:改一行、删一行都能定位到第几行。存于 Retain 类 PVC(误删 claim 不带走唯一的用量记录);dgx 上绑在头节点(D1,`.raid-verified` 哨兵同样适用)。
- **`billing/pricebook.yaml`**:系统里**唯一**写着价格的地方——offer 页的 $9.57/GPU·时 与 $52,750/月 此前不存在于任何配置。按 `effective_from` 版本化,金额用整数微美元,每 (sku, 租户类别) 一条永不打平。
- **`billing/invoice.py`**:确定性 CSV(同输入同字节)。按分钟**向上**取整、窗口裁剪、内部租户 $0、包月按天比例、**无生效价格的区间拒绝编造费率**(退出码 2 并标注 NOT PRICED)。
- Prometheus 抓取 `metering` 作为台账的**交叉核对**(chain_ok / 开放区间 / GPU 秒),永远不是计费来源。租户注册表第五个消费者,一致性门已覆盖。
- **MTR-01 入矩阵**:真实 GPU Pod 生命周期 → 恰好一对 open/close、链验证、指标累积、发票工具定价。
- 仍开放(等 D6):RentalAgreement 记录(期限/续约/价格快照)、欠费冻结机制、税/币种/折扣层。

### 2026-08-27 状态更新(WS6 落地:运维地基)

- **MAINTENANCE 进状态机**:第五个 owner 状态(CRD 三处 enum + CEL 兼容)。语义:计划停机——排空**所有**租户(含 DIRECT 常驻)、cordon、独立的 `arise.ai/maintenance` taint(租户 toleration 禁令天然覆盖);无卷门(所有权不变,节点会回来);出场必经 ARISE 回收的 sanitize+health(动过硬件后正该如此),或直接 DIRECT。有合同先等合同(unlist 不等于 reclaim)。稳态每周期重申隔离(OWN-06 同规)。**顺带修掉一个设计缺陷**:ARISE 回收的卷门原本挡"任何租户卷",这会让池内节点做一次维护就被自己的内部卷卡成单行道——改为只挡 **customer 租户**卷(离场客户的数据不许留给下一个住户;内部卷本就是池的合法住户)。console/SPA 全接入;MNT-01 在活 lab 全链路验证(排空真实 pod、防篡改、sanitize 出场)。
- **etcd 备份**:6 小时 CronJob(dgx overlay),save → `etcdutl snapshot status` 完整性校验 → 时间戳+保留 28 份。镜像与集群 etcd 静态 Pod **同 digest**。排练当场抓到 etcd 镜像 distroless 无 `/bin/sh` → 重构三段式(initContainers 纯参数跑 etcdctl/etcdutl,busybox 只做改名轮转);lab 实跑:12MB/699 键/哈希校验通过。`runbooks/etcd-restore.md` 含 kubeadm 恢复全流程与回退;**恢复演练本体与离节点副本仍开放**(后者等 D1/D2 定去处)。
- **机群健康(dgx-only,守"告警必须引用存在的序列")**:node-exporter(DaemonSet,声明式的布置豁免——可观测性必须上每台节点,容忍全部 arise.ai/* taint)+ kube-state-metrics;四条新告警:NodeNotReady(P0)、HostDiskLowOS(<100GB 绝对阈值)、HostDiskLowRAID(<1TB)、PodCrashLooping(平台命名空间)。
- **告警接收端防回退**:alertmanager 配置改为 **make 管理的 Secret**(与 code ConfigMaps 同模式)——否则 `make dgx-platform` 重放会把接好的 pager 静默打回 null sink。`make dgx-code` 播种 local-sink 且**只在缺失时**;`make dgx-alert-receiver WEBHOOK_URL=…` 一命令接真实端(URL 走 env 不走 argv,argv 在 ps 里全员可读)。verify-dgx 新增 DGX-21/22 三态检查(无配置=FAIL、local-sink=WARN、真实端=PASS;修掉过一版"无配置被当作真实端"的假通过)。
- **CHAOS-01 入矩阵**:VAST 交接中途 `kubectl delete pod` 杀控制器——重启后**同 transitionId** 恢复、marketplace 侧**恰好一次** list 副作用、节点最终回池。"卡住但安全"的持久化设计从此是演练过的事实。
- 四本事故 runbook:node-down(铁律:不许为恢复调度改 owner 面)、disk-full(/raid 是客户数据,默认动作是找人不是删文件)、gpu-fault(Xid 速查 + 走 MAINTENANCE)、quarantine-recovery(reason 表 + 放行三步,console 那句 "human-approved repair" 从此有定义)。

### 2026-08-27 状态更新(WS7 部分落地:Day-0 工具包)

- **测试矩阵现在可以直接打真机**。此前每条断言都焊死在 kind 上:`CTX="kind-${CLUSTER_NAME}"`,以及 33 处 `${CLUSTER_NAME}-workerN` 物理节点名。现在 `KUBE_CONTEXT` 可覆盖(沿用 `onboard-node.sh` 已有写法),物理名全部改由 `node_for <逻辑id>` 经标签解析(GPU 节点 `arise.ai/node-id`、辅助节点 `arise.ai/aux-name`、头节点 role 标签)。**这同时消除一个静默失败模式**:硬编码的名字在别的集群上不存在时 kubectl 返回空串,而"空==空"的断言会假通过。解析不到时 `node_for` 返回 `NODE-NOT-FOUND-<id>` 并非零退出。L0 §12 守住:tests/ 里再出现物理节点名即 FAIL。
- **`scripts/verify-dgx.sh`** — 硬件完成门,是 lab 版 `verify.sh` 的镜像:断真 GPU 存在且数量完整、**零** `arise.dev/*` 模拟容量、模拟插件与 vast-mock 命名空间**不存在**、七条准入绑定齐备、适配器/排空资源/网关公网模式/认证 Secret/配额绑真 GPU/存储类/租户注册表就位;DCGM 未装时 WARN 而非 FAIL(Day-0 步骤 7 之前缺席是正常的,之后缺席意味着 GPU 健康无人观测)。`KUBE_CONTEXT` 未设时**拒绝运行**——回落到 kind 会让"硬件门通过"成为谎言。反向验证:对 lab 集群运行返回 13 项失败、退出码 1,每一项都点名了正确的 lab-ism。
- **`infra/dgx/kubeadm-cluster-config.yaml`** — Day-0 步骤 4 的集群配置。CIDR 与 versions.env 对齐(租户出向策略按字面值拒绝这些网段,只改一处会静默打开租户→平台的 pod 间流量);审计日志、etcd 独立盘、kubelet 系统预留与驱逐阈值、只读端口关闭均已定;依赖 D1 的项标 ⟪DECIDE⟫,到货时是填空而非设计。
- **`make dgx-verify` / `make dgx-test`** 把上面两件接进同一套编排。
- 仍缺:registry 引导脚本、GPU/Network Operator 的 values(两者都需要与厂商确认 DGX OS 自带什么,plan §13.2 明确禁止 OS 与 Operator 同时管驱动)。

### 2026-08-27 状态更新(WS3 落地:租户入驻)

已关闭 1 项 P0、部分完成 1 项;开放:P0=30 P1=15 P2=0。

- **修掉一个 fail-open 的 P0**:`platform-internal-ingress` 原先靠**枚举租户命名空间名**排除租户。第三个租户的 ns 不在任何名单里 → 会被**允许**按 Pod IP 直连 tenant-portal / ops-console,完全绕过网关鉴权。现在以 `arise.ai/tier NotIn [tenant]` 标签为准(名字清单保留作腰带,两条表达式 AND)。`NotIn` 对缺失该键的命名空间同样匹配,正好覆盖 platform-system / monitoring / kube-system。
- **队列绑定改为标签驱动**:租户从 CEL 白名单里移出,改由命名空间自身的 `arise.ai/queue` 标签决定;基础设施命名空间(test-system 等需要多队列)保留显式清单。旧写法下,漏改 CEL 会把付费客户**静默降级到 default 队列**——没有权重、没有 reclaim 保护,即调度器可随时收回其算力。SCH-12 新增三条断言,直接用一个**任何 CEL 都没提到的命名空间**验证标签机制(正是新租户的处境)。
- **单一真相源**:`platform/tenants.yaml` 定义租户(命名空间/短名/队列/owner/优先级/种子账号),`scripts/tenants-json.py` 渲染成 `platform-tenants` ConfigMap,portal 与 gateway 挂载读取(缺失或损坏时回落到内置默认并告警,绝不 crash-loop 控制台)。
- **一致性门**(`scripts/tenant-check.py`,validate.sh §11):断言注册表与全部消费者一致——命名空间标签与 PSA、配额、LimitRange、三条 NetworkPolicy、租户 RBAC、两个 overlay 的 portal RBAC、队列对象与标签、portal/gateway 的回落清单、以及两个服务确实读取挂载文件。负向验证:临时注册一个虚构租户会精确列出 **18 处**未接线的位置。
- **生成器**:`scripts/onboard-tenant.py <ns>` 产出该租户全部 k8s 对象(命名空间含全部载荷标签、配额、LimitRange、三条围栏、租户与 portal RBAC),配额按 customer/internal profile 给**默认值并明确标注需按合同复核**。
- 入驻流程从"改 ~12 处硬编码"变成:改注册表 → 应用一份生成清单 → `make validate` 告诉你还差什么 → 控制台里建账号(账号不需要改代码,也不该有凭据进 Git)。
- **自审又挖出一个更深的洞并修掉**:`capacity-controller` 与 `ops-console` 里的 `TENANT_NAMESPACES` 仍是硬编码元组。它不是文档问题,而是**排空逻辑的输入**——新入驻租户对排空**不可见**,其 Pod 会在节点被上架 VAST 或回收进 ARISE 池时**继续跑在已售出的硬件上**。两个服务现已读注册表,一致性门也补上了这两个文件(负向验证:把控制器的元组改回单租户,门会精确指出"其 Pod 不会在所有权交接时被排空")。
- **DIRECT 预留现在必须说明"为谁"**:原逻辑把"内部租户"写死成 `("tenant-arise",)`,多客户下会把客户 Y 的负载留在客户 X 付费的节点上。CRD 新增 `spec.tenant`(预留给哪个租户命名空间);单一 customer 租户时可推断,**两个及以上时控制器拒绝猜测**、停在 DRAINING 并写 `AmbiguousReservation` 条件(符合本控制器"卡住但安全 > 自信地做错"的一贯规则)。卷门同样改为"除该租户外的所有租户"。5 条新单测覆盖显式/推断/歧义/未知租户/端到端不变更。
- 仍开放:租户出向 NetworkPolicy 的 CIDR 例外表仍是 kind/AWS 硬编码(需 D1/D4 定网络后按 overlay 参数化);配额 profile 的数值需按合同复核;`spec.tenant` 目前由运维在预留时填写,尚未与合同/计费记录关联(WS5)。

---
## 0. 八个必须先拍板的决策(阻塞面最大,今天就该定)

多个维度的 DECISION 项去重后是 8 个真正的决策,每个都点名了被它卡住的工作:

| # | 决策 | 卡住什么 |
|---|---|---|
| D1 | **头节点方案**:控制面/etcd/gateway/监控/registry 放哪?4 台 DGX 全要卖,须加一台 CPU 头节点(建议:1–2 台普通服务器) | kubeadm 拓扑、dgx overlay 的调度约束、整个 Day-0 步骤 4 |
| D2 | **存储架构与承诺**:30TB 本地 NVMe 卖给客户——单盘裸奔还是 RAID/复制?数据丢失责任怎么写? | 存储类清单、备份方案、offer 页的诚实性 |
| D3 | **客户身份**:自建认证升级 vs 接 OIDC/IdP(Auth0/Keycloak/…)?注册、改密、MFA 谁提供? | gateway 改造方向、会话持久化方案、入驻流程 |
| D4 | **公网边缘**:域名、LB/边缘节点、DDoS 姿态、TLS 在哪终结? | 边缘清单、证书自动化、Day-0 切流步骤 |
| D5 | **按需池**:$9.57/卡·时的按分钟客户跑在哪个 owner 池、隔离粒度(整节点/单卡/命名空间)? | 入驻自动化、准入策略泛化、计量台账的记账主体 |
| D6 | **收款与记账**:支付通道、记账系统、预付 vs 后付、租户计费分类? | 计量 schema 最后一公里、发票、欠费冻结机制 |
| D7 | **隔离披露**:共享内核、无 VM 隔离——对客户怎么诚实披露、条款怎么写? | 客户合同、offer 页措辞、安全边界文档 |
| D8 | **IB 织物管理**:subnet manager 跑在哪(交换机托管 vs 主机 opensm)、谁负责? | 织物拉起脚本、双节点 XDR 验收测试 |

---
## 1. 六维结论(每维一段,原文)

### 硬件拉起(hardware-bringup)

The control-plane rehearsal is genuinely strong — onboarding, ownership state machine, admission gates and queues are tested paths that transfer — but everything between power-on and that control plane existing is unwritten: no cluster bootstrap, a dgx overlay that renders zero workloads, no way to move an image onto a real node, no GPU/IB/storage manifests, a completion gate that asserts the GPUs must NOT exist, and open decisions (control-plane placement, NVMe layout, fabric management, driver ownership) that block the pre-writing the prime directive demands; the honest distance is that roughly the lower half of the stack does not exist yet, though most of it is pre-buildable this week. Day-0 runbook skeleton (R = pre-buildable/rehearsable on this EC2 now): 1) rack/power/BMC access + firmware inventory vs written baseline (R: checklist/script); 2) DGX OS validation, NVMe layout, nvidia-smi + field diag per node (R: scripts only); 3) fabric bring-up — cabling plan, switch/subnet-manager config, ibstat link-rate checks (blocked on fabric DECISION; R: check scripts); 4) admin/control-plane host up (blocked on placement DECISION), containerd + kubeadm init from pre-written ClusterConfiguration reusing POD_CIDR/SERVICE_CIDR, vendored CNI applied, 4 nodes joined (R: every manifest and script); after CNI: RE-VERIFY kubelet probes pass the platform-internal-ingress policy (kindnetd verification does not transfer — see networkpolicies.yaml); 5) private registry up, digest-pinned image set mirrored and verified (R: fully rehearsable via kind+local-registry pattern); 6) `make dgx-deploy` (EXISTS since 2026-08-26: dgx-render → dgx-platform → dgx-code) — completed dgx overlay + monitoring + storage classes are BUILT; vendored Volcano still to add (R: rehearsable as a dgx-sim overlay on the kind cluster); 7) GPU Operator with the decided driver flags → nvidia.com/gpu=8 per node, DCGM scraping and alerts live (R: values + helm template render); 8) Network Operator + RDMA resources → NCCL 2-node all-reduce and single-node NVLink bandwidth meet the contractually written thresholds (R: vcjob manifests + thresholds); 9) NVMe storage classes PRE-BUILT (arise-shared/arise-longterm on local-path@digest at /raid; per-node step: verify RAID mount then `sudo install -d /raid/arise/volumes && sudo touch /raid/arise/volumes/.raid-verified` — provisioning fails closed without it); fio baseline recorded, real sanitize path timed (D2 still owns the durability promise); 10) `onboard-node.sh gpu dgx01..04` — the one step already rehearsed on every matrix run (NODE-01); 11) verify-dgx.sh completion gate + KUBE_CONTEXT-parameterized smoke matrix against the real cluster (R: build and dry-run now); 12) TLS-terminated ingress + seeded credentials replaced before any external exposure (credentials: `make dgx-gateway-secret` BEFORE `make dgx-deploy` — the gateway will not start without it; at cutover flip GW_COOKIE_SECURE and GW_TRUST_PROXY to "true" together in platform/overlays/dgx/gateway.yaml, never one without the other); 13) HW-01..15 acceptance matrix executed and evidenced; 14) end-to-end DIRECT-reservation dry run before the first paying customer. Steps 4-8 and 11 are the pre-build backlog; steps 5, 6 and 11 can be rehearsed to completion on this machine before the hardware ships.

### 公网暴露(public-exposure)

In this dimension the platform is honest but essentially at zero: the entire public path — TLS, certificates, ingress, DNS, DDoS posture, even a production deployment of the gateway itself — exists only as comments promising "the real machines will have it", and the dgx overlay ships no serving workload at all. The gateway is a competent prelab aggregator but a dev-server-grade public listener: unbounded thread-per-connection with no timeouts, default admin passwords live in the rehearsed config, unthrottled 120k-iteration logins, and all users/sessions in one process's memory, which also forbids HA. The good news is that roughly three quarters of the distance is closable on the EC2 today (edge manifests rehearsed in kind, server hardening, fail-closed secrets, throttling, persistent/stateless sessions, logging, cookie/CSRF fixes are all S/M efforts), but two business decisions — the customer identity provider and the edge/domain/DDoS topology — gate the rest and are not yet owned by anyone; until they land, the day-0 cutover cannot even be scripted, and launch speed will be set by whichever decision is made last.

### 客户产品链(tenant-product)

The control plane underneath is genuinely rehearsed, but the paying customer's path itself does not exist yet: every link a customer would personally touch is either missing or dead-ends. Onboarding tenant #3 requires coordinated edits to roughly twelve hardcoded lists (one of which — the platform-ingress fence's enumerated NotIn — fails open for an unlisted tenant); a rented dev machine is a sleep-loop pod with no SSH, exec, or tunnel and a 20KB log tail as the product's entire I/O surface; there is one frozen CPU image and no registry; results can never leave the cluster; the inference product creates endpoints nothing can connect to; and the per-minute price on the offer page has zero metering, ledger, or billing code behind it, with every candidate data source volatile. The good news for the prime directive: seven of nine gaps are fully pre-buildable and rehearsable on this EC2 today in kind — the true blocker to start this week is a single business decision (which owner pool serves on-demand customers and at what isolation granularity), because the onboarding automation, admission-policy generalization, and metering ledger all take shape around that answer.

### 计费计量(billing-metering)

In the billing and metering dimension the distance to a paid public launch is total: the repo contains not one line of code, config, or test that measures, prices, records, or invoices anything — no per-tenant usage series exists or is derivable (three fleet-level scrape jobs, no kube-state-metrics/cAdvisor), the only time-series store is 3-day emptyDir Prometheus that the repo itself correctly refuses to treat as a source of truth, the offer-page prices appear nowhere in the system, the only contract model is the VAST marketplace mock (customer dedicated rentals have no record of customer, term, price, or renewal), and there is no suspension lever for a non-paying tenant. The mitigating fact is that nearly all of it is EC2-NOW pre-buildable within the house rules: the codebase already has every ingredient the minimal honest skeleton needs — a proven stdlib watch/reconcile pattern, an idempotency discipline (transitionId/OWN-04) that maps directly onto exactly-once usage intervals, a hash-evidence habit for a tamper-evident ledger, Retain-class PVCs to hold it, VAP admission gates to enforce suspension, and a 34-case test matrix to bolt MTR/BIL/SUS cases onto. One metering service (sole writer of an append-only, hash-chained allocation ledger keyed on pod UID, exporting per-tenant gauges for Prometheus cross-check), a versioned price book, a deterministic invoicer, a RentalAgreement record wired into the DIRECT flow, and a rehearsed freeze runbook constitute roughly two to three weeks of focused work — but the last mile is gated by one genuinely un-codeable decision (merchant of record, payment rails, prepaid vs postpaid, and the tenant billing-class mapping) that should be made now, because the metering schema depends on it.

### 可靠性运维(reliability-ops)

The control-plane logic itself is in unusually good shape for a prelab (durable transition state in a ConfigMap, idempotent transitionIds, readback recovery, drift correction — all matrix-tested), but everything AROUND it that a paid service stands on does not exist yet: the production (dgx) overlay ships zero workloads and the deploy path is kind-load/docker-cp lab-isms; there is no head-node plan, so on 4 sellable DGX nodes there is literally nowhere designed for the API server, etcd, gateway or Prometheus to live; nothing is backed up (no etcd snapshot, no velero, monitoring on emptyDir, users/sessions in process memory); alerts terminate in a null receiver and there is no node-down, disk-full or GPU-fault detection path for real hardware; customer data durability on local NVMe is an unmade decision already contradicted by the offer page (30TB included, Retain-class semantics local-path cannot honor); upgrade means teardown+rebuild and there is no maintenance state in the ownership machine; and only 6 runbooks exist, none of them incident runbooks. Real distance to launch in this dimension: roughly 5 large pre-buildable work packages (production overlay + registry, backup/restore, alerting/paging + fleet-health coverage, upgrade/maintenance procedure, incident runbooks + chaos rehearsal) all doable on EC2 now, two hardware-day items that can be pre-written (DCGM/Xid pipeline, Loki), and three business decisions (head-node hardware, identity provider, storage durability SLA) that block everything downstream of them and should be forced this week.

### 上线安全(security-launch)

The internal posture genuinely earned its rehearsal value — least-privilege RBAC split, PSA restricted tenants, CEL admission gates, empirically-probed NetworkPolicies, digest-pinned upstream images, and two real audits with confirmed fixes — but every one of those controls assumes the perimeter is a trusted 127.0.0.1 port-forward, and nothing that faces the internet exists yet: default passwords are the live credentials with no Secret machinery in the repo, there is no TLS or ingress artifact of any kind, login is unthrottled brute-forceable CPU amplification with no password-change path, the audit trail dies with the pod, and the overlay that actually ships to hardware has drifted so far from the rehearsed one that it would deny the paid DIRECT product's own workloads while omitting the queue-entitlement gate. The honest distance is roughly: four P0 build items that are fully pre-buildable and rehearsable on this EC2 today (secrets + fail-closed defaults, TLS front, overlay policy unification, CIDR parameterization), one P0 runbook (host/BMC hardening) that can be written before hardware, and three owner decisions that gate the rest — IdP vs hardened built-in auth, image registry, and the sub-node co-tenancy/isolation-disclosure question that the "NVLink guaranteed in writing" offer makes contractual. None of this requires the machines; all of it stops being schedule-compatible with "as fast as possible" if it is still open on delivery day.

---
## 2. 完整台账

### P0(42 项)

| 维度 | 位置 | 量级 | 差距 | 为什么阻塞 | 第一步 |
|---|---|---|---|---|---|
| 计费计量 | 需拍板 | S | **DECISION: payment rails, billing system of record, and the billing cla** | Code cannot answer: who is the merchant of record; invoice-and-wire vs card (Stripe etc.); postpaid net-30 vs  | Owner decides and writes billing/BILLING-MODEL.md: merchant of record, payment rail, prepaid-credit  |
| 硬件拉起 | 需拍板 | L | **Storage sells 30TB NVMe per node; the repo implements kind local-path ** | The offer page promises 30TB included storage and no IOPS metering; both storage classes are provisioner ranch | Put the decision to the operator: E1.S layout (RAID0 /raid vs per-drive local PVs), which local prov |
| 硬件拉起 | 需拍板 | S | **Undecided: where the control plane, gateway, monitoring, and registry ** | The delivered hardware is 4 DGX B300 nodes — the cpu01/02 and stor01 nodes the whole simulated topology (and g | Put a costed either/or to the operator this week: (a) procure 1-2 CPU admin nodes to host control pl |
| 公网暴露 | 需拍板 | M | **DECISION: customer identity — who provides signup, password reset, MFA** | Every runbook waves auth away as 'replaced by OIDC/SSO on the real machines', but no IdP is chosen, no owner n | Put the decision to the operator this week: managed IdP (e.g. Auth0/Cognito/Keycloak self-hosted) vs |
| 公网暴露 | 需拍板 | S | **DECISION: public edge topology, domain, and DDoS posture for 4 bare-me** | Nobody has decided what actually fronts the fleet: which domain, where the public IP lives, whether a cloud LB | Write a one-page options memo for the operator: (a) CDN/proxy in front (DDoS absorbed, TLS at edge + |
| 可靠性运维 | 需拍板 | M | **No head-node plan: control plane, etcd and all platform services have ** | The lab simulates 1 control-plane + workers, and the gateway is hard-pinned to that control-plane node. On the | Decision for the operator: order head-node hardware before the DGXs arrive — 1 head node (accept etc |
| 可靠性运维 | 需拍板 | M | **Customer data durability on local NVMe is an unmade decision already c** | The offer sells 30TB included storage; the platform's arise-longterm class advertises Retain semantics ('data  | Decision for the operator, before the offer page hardens: (a) the written durability SLA per storage |
| 可靠性运维 | 需拍板 | M | **Customer accounts and sessions live in gateway process memory; identit** | USERS and SESSIONS are Python dicts: every gateway restart (including the routine rollout that ships any fix)  | Decision: name the IdP (hosted Auth0/WorkOS-style vs self-hosted Keycloak on the head node) and wher |
| 上线安全 | 需拍板 | L | **Gateway auth is not internet-grade: no throttling/lockout, no password** | The login path accepts unlimited guesses (gaps.md explicitly defers throttling) and each guess costs the serve | Decision to name an owner for: OIDC/SSO provider (self-hosted Dex/Keycloak vs SaaS IdP) vs hardening |
| 上线安全 | 需拍板 | M | **Isolation limits of the offer are undecided and undisclosed: shared ke** | Tenant workloads are containers (PSA restricted, RuntimeDefault seccomp, non-root — good), not VMs: shared ker | Owner decides: (a) is cross-tenant sub-node co-residency offered at launch at all — recommended defa |
| 客户产品链 | 需拍板 | M | **DECISION: which owner pool serves public on-demand customers, and how ** | The owner state machine knows ARISE (internal) / DIRECT (whole-node reservation, matches the $52,750/mo produc | Put the named decision to the operator: (a) does on-demand get its own owner class (e.g. ONDEMAND) w |
| 硬件拉起 | 到货才做 | L | **No bare-metal Kubernetes bootstrap path (kubeadm init, containerd, CNI** | Every runbook presumes a running cluster: machine-registration.md's precondition is 'kubeadm cluster + dgx ove | Write infra/dgx/kubeadm-init.yaml (ClusterConfiguration pinning k8s v1.36.2, podSubnet=10.244.0.0/16 |
| 硬件拉起 | 到货才做 | L | **IB/XDR and NVLink are sold in writing with zero fabric config and zero** | The offer page prices a two-node XDR InfiniBand option (16-GPU jobs) and guarantees NVLink in writing, yet the | Pre-write the acceptance jobs now: an NCCL all-reduce vcjob pair (single-node NVLink, two-node IB) w |
| 公网暴露 | 到货才做 | M | **Day-0 cutover runbook for DNS, real certs, firewall, and Secure-cookie** | Even with the edge stack pre-built, launch day needs an exact, rehearsed sequence: create DNS records, obtain  | Write runbooks/public-cutover.md now with the literal commands (DNS record spec, cert-manager Issuer |
| 可靠性运维 | 到货才做 | M | **GPU-fault path on real hardware does not exist: fault API is lab-only ** | Today GPU health is the /test/unhealthy injection API on the lab-only advertiser, and the one GPU alert is exp | Pre-write now: dcgm-exporter DaemonSet manifest (digest-pinned, dgx overlay), Prometheus scrape + al |
| 上线安全 | 到货才做 | M | **〔PARTIAL 2026-08-27:node-bootstrap.sh 含 sshd 加固(key-only/no root pw)与 sysctl;完整基线(防火墙/审计 daemon/完整性)仍开放〕DGX host OS hardening baseline does not exist: no SSH policy, no host ** | machine-registration.md's host section covers OS image, containerd and kubeadm join only. Nothing anywhere spe | Write runbooks/host-hardening.md now (pre-hardware): sshd_config drop-in (key-only, no root, AllowUs |
| 计费计量 | 现在可建 | M | **〔DONE 2026-08-27:services/metering 分配台账,MTR-01 活集群验证〕No per-tenant usage metering exists anywhere — GPU-time is never recor** | The offer sells $9.57/GPU-hr with per-minute billing, but no component records which tenant held which GPU for | Create services/metering/metering.py (stdlib, following the capacity_controller.py api() watch patte |
| 计费计量 | 现在可建 | M | **〔DONE 2026-08-27:append-only JSONL + sha256 链,Retain 类 PVC,篡改/删行可定位到行〕No durable, auditable usage ledger — the only time-series store is 3-d** | Per-minute billing on a monthly invoice needs a tamper-evident record surviving at least ~40 days and every re | Give the metering service an append-only JSONL ledger on an arise-longterm PVC (Retain class already |
| 计费计量 | 现在可建 | S | **〔DONE 2026-08-27:billing/pricebook.yaml,按 effective_from 版本化,整数微美元〕Price book exists only on the external offer page — no price is repres** | $52,750/mo and $9.57/GPU-hr appear in no config, no service, no doc in the repo. Without a versioned, effectiv | Add billing/pricebook.yaml: versioned entries {sku, unit, unit_price_usd, effective_from} covering o |
| 计费计量 | 现在可建 | M | **〔PARTIAL 2026-08-27:CRD spec.tenant 记录'为谁预留';发票按 --dedicated-days 按比例计费;RentalAgreement(期限/续约/价格快照)仍开放,等 D6〕Dedicated-node rentals ($52,750/mo) have no customer, term, or renewal** | NodeOwnership DIRECT records that a node is reserved, but not for whom, since when, at what price, or until wh | Add a RentalAgreement record (new small CRD or ledger-backed, single-writer: the billing/metering se |
| 硬件拉起 | 现在可建 | L | **〔DONE 2026-08-26〕dgx overlay renders zero workloads — the entire product stack is lab-o** | Switching overlay is the documented promotion path, but `kubectl kustomize platform/overlays/dgx` emits only n | Extract the hardware-portable pieces (gateway, portal, console, capacity-controller, monitoring, gra |
| 硬件拉起 | 现在可建 | M | **〔PARTIAL 2026-08-27:web/devbox 内容镜像 + registry-mirror.sh(排练过)+ verify-dgx DGX-23/24 哨兵 retag 检查;registry 落点(头节点 docker vs 集群内)属 D1,vendored Volcano 仍未建〕No registry or image-delivery story — kind load and docker cp die with** | Every byte that reaches a node today travels a kind-only path: the fake-gpu plugin via `kind load` with imageP | Vendor the Volcano installer manifest into the repo with images rewritten to digests; stand up the d |
| 硬件拉起 | 现在可建 | M | **〔PARTIAL 2026-08-27:infra/dgx/operators/ 两份 values 草案,可拍板项标 ⟪DECIDE-WITH-VENDOR⟫;driver.enabled 必须看到实机再定〕GPU Operator integration is a version number and a comment — no manife** | Real GPUs appear as nvidia.com/gpu only if the GPU Operator (or DGX OS native stack) is deployed; the repo has | Write infra/dgx/gpu-operator-values.yaml in two variants (driver.enabled true/false) plus the exact  |
| 硬件拉起 | 现在可建 | M | **〔DONE 2026-08-27:KUBE_CONTEXT 参数化 + node_for() 逻辑名解析 + verify-dgx.sh;L0 §12 防回潮〕Completion gate and test harness are hardwired to the lab shape — and ** | verify.sh asserts exactly 8 nodes, 32 fake-gpus, 1152 sim-vCPUs, and — SEC-03a — that total nvidia.com/gpu equ | Thread KUBE_CONTEXT through verify.sh, tests/lib.sh and label-nodes.sh, then write verify-dgx.sh (no |
| 公网暴露 | 现在可建 | M | **〔PARTIAL 2026-08-27:platform/overlays/dgx/edge/ 独立 kustomization(ingress-nginx hostNetwork@digest + cert-manager values@digest + ClusterIssuer + Ingress),域名/邮箱/LB 形态标 ⟪DECIDE-D4⟫;故意不进 dgx-deploy〕No edge stack exists: TLS termination, ingress, and cert automation ar** | A public paid service cannot ship passwords and session cookies over plaintext HTTP. Today the only access pat | Create platform/overlays/dgx/edge/ with digest-pinned ingress-nginx + cert-manager manifests (versio |
| 公网暴露 | 现在可建 | M | **〔DONE 2026-08-26(TLS/edge 仍开放,见公网维其余行)〕dgx overlay ships zero front-door workloads; SPA delivery is a pure la** | overlays/dgx has no gateway, portal, console, or any mechanism to serve the SPA — the lab delivers web/dist by | Build a proper gateway image with web/dist baked in (replacing configMap-code + hostPath), digest-pi |
| 公网暴露 | 现在可建 | S | **〔DONE 2026-08-27〕Gateway is not survivable as a public HTTP server (slowloris, unbounde** | Handler sets no socket timeout, so one client holding a connection open (or trickling a body into rfile.read,  | In gateway.py set Handler.timeout = 15 and protocol_version = 'HTTP/1.1', and wrap ThreadingHTTPServ |
| 公网暴露 | 现在可建 | S | **〔DONE 2026-08-27〕Seed accounts fall back to hardcoded default passwords (admin/arise-ad** | If GW_ADMIN_PASSWORD is unset the public front door has a documented admin password; the lab deployment sets n | Make seed_users() raise and exit if any GW_*_PASSWORD is unset or equals a known default when GW_PUB |
| 公网暴露 | 现在可建 | S | **〔DONE 2026-08-27〕No login rate limiting, lockout, or throttling — credential stuffing p** | POST /auth/login is unauthenticated and every attempt burns ~120k PBKDF2 iterations inside a pod capped at 300 | Add a stdlib in-process limiter to _auth: per-source-IP (from a trusted X-Forwarded-For once behind  |
| 公网暴露 | 现在可建 | M | **〔PARTIAL 2026-08-27:会话已无状态签名（重启/多副本可用）；用户表仍在进程内，待 D3〕Users and sessions live in gateway process memory: restart wipes custo** | USERS and SESSIONS are plain module-level dicts. Any deploy, crash, or node reboot logs out every customer and | Replace the session dict with stateless signed tokens (HMAC over user∣role∣tenant∣exp with a key fro |
| 可靠性运维 | 现在可建 | L | **〔DONE 2026-08-26〕Production (dgx) overlay ships zero workloads; deploy path is lab-only** | Nothing customer-facing can be installed on real hardware today: the dgx overlay contains no gateway, tenant-p | Refactor the lab workload manifests into shared kustomize components consumed by both overlays (lab  |
| 可靠性运维 | 现在可建 | M | **〔DONE 2026-08-27:6 小时 CronJob + 完整性校验 + 恢复 runbook;备份**与恢复**均在 lab 实跑(API 6 秒内恢复、所有权世界逐条一致);仅离节点副本等 D1/D2 定去处〕Zero backup of cluster state: no etcd snapshots, no restore procedure,** | Every piece of durable state the platform has — NodeOwnership CRs (who owns a $52k/mo node), the capacity-cont | Add an etcd snapshot CronJob (etcdctl snapshot save to a volume that leaves the node, plus `kubectl  |
| 可靠性运维 | 现在可建 | S | **〔DONE 2026-08-27(接收端 URL 本身仍待 owner 定;结构已一命令化且防重放回退)〕Alertmanager routes every alert, including P0 OwnerConflict, to a null** | The alert rules are genuinely good (OwnerConflict, ContractReclaimAttempt, ControllerDown) but they terminate  | Pick the paging provider (small decision: PagerDuty/Opsgenie/Slack webhook + phone escalation) and w |
| 上线安全 | 现在可建 | M | **〔DONE 2026-08-27:Secret 注入 + fail-closed 启动;密钥永不入 Git〕Default GW_* passwords are the live production auth and no secrets mec** | gateway.py seeds admin/arise-admin, arise-dev, direct-cust when env is unset, and the deployed manifest sets n | Make gateway.py fail closed: refuse to serve /auth/login for any account whose password came from a  |
| 上线安全 | 现在可建 | M | **〔PARTIAL 2026-08-27:同上,边缘已预建;切流时 GW_COOKIE_SECURE/GW_TRUST_PROXY 同时翻转〕No TLS anywhere and no external exposure layer exists — credentials an** | Every hop is plain HTTP; the session cookie is set without Secure; there is no Ingress, LoadBalancer, cert, or | Add a TLS-terminating front (ingress-nginx pinned by digest, or an nginx deployment) to a new shared |
| 上线安全 | 现在可建 | M | **〔DONE 2026-08-26:owner-gate DIRECT 分支、host-isolation、queue/priority/flavor/storage 全部入 dgx,render-check 断言防再漂移〕dgx overlay has drifted from the rehearsed policy set — it would deny ** | Kustomize-building both overlays shows dgx is missing arise-queue-binding (any tenant can claim the paying cus | Extract the hardware-agnostic policies (owner-gate with the DIRECT branch, host-isolation, queue-bin |
| 上线安全 | 现在可建 | S | **〔DONE 2026-08-27:入向围栏标签驱动;出向在 dgx 改为 fail-closed(拒全部 RFC1918+链路本地+pod/svc,租户只达公网——不猜数据中心网段,猜错即 fail-open)〕Tenant egress NetworkPolicy fence hardcodes kind/AWS CIDRs — the fixed** | The deny-imds-and-host-links except-list is literally kind's podSubnet (10.244.0.0/16), the AWS VPC (172.31.0. | Move the CIDR except-lists out of base into per-overlay patches; create platform/overlays/dgx/networ |
| 客户产品链 | 现在可建 | L | **〔DONE 2026-08-27:注册表 + 标签驱动 + L0 一致性门;入驻 = 加一条目 + 应用一份生成清单〕Tenant onboarding is impossible without editing ~12 hardcoded lists ac** | A public service must turn a signed-up startup into a working tenant (namespace, quota, netpols, queue, queue- | Convert discovery to the namespace label that already exists (arise.ai/tier=tenant, namespaces.yaml: |
| 客户产品链 | 现在可建 | L | **〔DONE 2026-08-27(集群内半段):devbox 镜像 + 公钥注入 + 非 root sshd + scp/rsync,ACC-01;公网接入地址仍是 D4〕No path for a customer to reach their own dev machine — no SSH, no exe** | This is the existential link for a rental product: today a paying customer can create a pod that runs a sleep  | Write the access ADR (per-pod sshd + TCP edge/bastion vs web-terminal-via-exec — sshd+bastion recomm |
| 客户产品链 | 现在可建 | L | **〔DONE 2026-08-27:与上面同一台账〕No mechanism records per-minute GPU attach/release — the $9.57/hr on-d** | The offer page sells per-minute billing, but no code anywhere records when a tenant held a GPU. Attach/release | Build a stdlib metering service now: watch pods in tenant namespaces holding the GPU extended resour |
| 客户产品链 | 现在可建 | L | **〔PARTIAL 2026-08-27:目录加 devbox 镜像;scripts/registry-mirror.sh 把全部 pinned 镜像进私有 registry(本地 registry 排练过,digest 逐个相同);租户自带镜像(BYO)路径仍开放——需要 registry 鉴权与镜像审查策略〕Single frozen catalog image, no registry, no bring-your-own-image path** | A GPU rental customer must run their own CUDA/PyTorch containers; today the portal hard-rejects any image exce | Deploy a digest-pinned registry:2 in the lab overlay with per-tenant htpasswd robot accounts and per |
| 客户产品链 | 现在可建 | M | **〔DONE 2026-08-27:scp/rsync/sftp 经同一 sshd〕No data path in or out: customers cannot upload a dataset or download ** | The complete data-out surface of the product is a 20KB log tail; there is no upload endpoint, no object storag | Decide jointly with gap #2: if SSH lands, scp/rsync covers files day one — write that into the acces |

### P1(17 项)

| 维度 | 位置 | 量级 | 差距 | 为什么阻塞 | 第一步 |
|---|---|---|---|---|---|
| 公网暴露 | 需拍板 | S | **Admin plane (ops-console, Prometheus, Alertmanager, user management) r** | The gateway proxies /oapi (node-ownership intents), /prom (arbitrary PromQL), /am, and /auth/users on the very | Decide with the operator: split /oapi, /prom, /am, /auth/users onto a second ingress hostname restri |
| 上线安全 | 需拍板 | M | **〔PARTIAL 2026-08-27:registry 镜像流程已排练;lab 专属镜像明确排除;controller 的 ConfigMap 规则收窄仍开放〕Supply chain unfinished for a real registry, and cluster-wide ConfigMa** | Product code reaches the cluster as ConfigMap-mounted Python inside an upstream python image; the Go plugin is | Two-part: (immediate, EC2-NOW, S) replace the controller's cluster-wide ConfigMap rule with a namesp |
| 计费计量 | 到货才做 | S | **〔PARTIAL 2026-08-26:30Ti 配额 patch 已写入 dgx overlay;计量部分仍开放〕Storage entitlement ('30TB included') and storage usage are unrepresen** | The offer includes 30TB per dedicated node with no IOPS metering — the honest minimum is (a) the entitlement e | Write the dgx-overlay ResourceQuota patch now with the offer-page numbers (30Ti requests.storage per |
| 硬件拉起 | 到货才做 | M | **〔DONE 2026-08-27:infra/dgx/node-bootstrap.sh 幂等脚本(sysctl/swap/containerd/RAID 哨兵/审计目录/sshd);kubeadm join 故意不内嵌 token〕Host-layer bring-up is a manual 4-row table (DGX OS, firmware, contain** | machine-registration.md §1 is honest that the host layer is 'the only manual segment, ~30 min per machine, Ans | Write an idempotent per-node bootstrap script (stdlib bash, matching house style) that captures firm |
| 计费计量 | 现在可建 | M | **〔DONE 2026-08-27:billing/invoice.py 确定性 CSV;按分钟向上取整、窗口裁剪、内部 $0、无价拒绝编造〕No invoice generation or export path — not even CSV** | Even with usage recorded and prices configured, nothing turns them into a customer-facing statement. A paid pu | Write billing/invoice.py (stdlib): reads the usage ledger + pricebook.yaml for a [start,end) window, |
| 计费计量 | 现在可建 | M | **No suspension / grace / freeze mechanics for a non-paying tenant** | When a customer stops paying, the platform has no lever short of manually deleting their workloads: quotas are | Build and rehearse a tenant-freeze runbook + script: step 1 label the namespace arise.ai/suspended=t |
| 公网暴露 | 现在可建 | S | **〔DONE 2026-08-27〕Cookie and CSRF posture unfit for a real domain: no Secure/__Host-, Sa** | On a public domain the session cookie will be transmitted on any http:// fetch (no Secure flag), is settable b | Rename the cookie to __Host-arise_session with Secure gated on a GW_COOKIE_SECURE env (on in dgx ove |
| 公网暴露 | 现在可建 | S | **〔DONE 2026-08-27〕Zero access logging and no client-IP awareness — no forensic trail for** | log_message is deliberately silenced and nothing reads X-Forwarded-For, so a public gateway would record no re | Implement log_message (or a wrapper in _send) emitting the existing JSON log shape with client IP (t |
| 可靠性运维 | 现在可建 | M | **〔DONE 2026-08-27(dgx overlay;lab 保持 guard.sh 补偿控制以守'告警必须引用存在的序列')〕No fleet-health monitoring: no node-exporter, no kube-state-metrics, n** | The scrape config covers the capacity-controller, the fake-gpu advertiser and kubelet metrics — nothing else.  | Deploy digest-pinned node-exporter (DaemonSet) and kube-state-metrics into the monitoring namespace, |
| 可靠性运维 | 现在可建 | L | **〔DONE 2026-08-27:MAINTENANCE 进状态机 + runbooks/upgrade-rollback.md 三层变更流程(服务/清单/节点/控制面),层 0/2 已演练〕No upgrade or rollback procedure for a live cluster — only teardown+re** | The only rehearsed change procedures destroy the cluster (rebuild-verify.sh, make teardown); runbooks/rollback | Write and rehearse in the lab: (1) a service-upgrade procedure (ConfigMap apply + ordered rollout re |
| 可靠性运维 | 现在可建 | M | **〔DONE 2026-08-27:CHAOS-01 入矩阵——中途杀 pod,同 transitionId 恢复、恰一次 list 副作用〕Controller death mid-transition is designed for but never rehearsed, a** | The durable-state design is sound (transition state in a ConfigMap, phase in CR status, idempotent transitionI | Add the missing chaos case (real OWN-07): start a VAST handover, `kubectl delete pod` the controller |
| 可靠性运维 | 现在可建 | S | **Monitoring state is disposable (emptyDir, 3-day retention) — no incide** | Prometheus, Alertmanager and Grafana all run on emptyDir with 3-day TSDB retention: any pod reschedule wipes a | Give Prometheus and Alertmanager PVC-backed storage in the shared manifests (small in lab, sized pro |
| 可靠性运维 | 现在可建 | M | **〔PARTIAL 2026-08-27:四本事故 runbook 落地(node-down/disk-full/gpu-fault/quarantine-recovery);日志聚合仍开放〕No incident runbooks and no log aggregation: node-dead, disk-full, qua** | runbooks/ holds six documents — lab-host install/rollback, AWS inventory, machine registration, the gaps regis | Write and rehearse the four highest-probability runbooks against the lab (node-dead = docker stop a  |
| 上线安全 | 现在可建 | M | **〔PARTIAL 2026-08-27:infra/dgx/audit-policy.yaml(11 条规则:所有权/节点/租户对象 RequestResponse,Secret 仅 Metadata);kubeadm 配置已引用;日志离节点仍开放〕No durable audit trail: stdout-only logs, no apiserver audit policy, n** | Every service logs JSON to stdout with pod-lifetime retention; metrics keep 3 days; there is no kube-apiserver | Three pre-buildable pieces: (1) write audit-policy.yaml (Metadata level for secrets, RequestResponse |
| 客户产品链 | 现在可建 | S | **〔DONE 2026-08-27:每台开发机 /home/dev 与 /tmp 为 emptyDir,rootfs 保持只读〕Dev-machine runtime cannot actually do work: read-only rootfs, no writ** | pip install / git clone / conda all fail on first write: the container has readOnlyRootFilesystem with no empt | Add emptyDir mounts for /tmp and a writable home to the devmachine pod spec in tenant_portal.py; add |
| 客户产品链 | 现在可建 | M | **The inference-service product creates endpoints nothing can reach** | The portal and SPA ship an online-services product that creates a Deployment + ClusterIP and advertises name.n | Either hide the Services module until it works, or make it real in kind: netpol template allowing in |
| 客户产品链 | 现在可建 | M | **〔DONE 2026-08-27:docs/customer/quickstart.md,只写今天真实可用的路径〕Zero customer-facing documentation** | docs/ holds one internal architecture mapping (Chinese, prelab-scoped) and the README is machine-ops for this  | Write docs/customer/quickstart.md against the portal API exactly as it exists (login → devmachine →  |

### P2(1 项)

| 维度 | 位置 | 量级 | 差距 | 为什么阻塞 | 第一步 |
|---|---|---|---|---|---|
| 硬件拉起 | 现在可建 | S | **〔DONE 2026-08-26〕base leaks lab-only objects (vast-mock namespace/quota, fake-gpu-plugi** | The dgx kustomization's header promises vast-mock and fake-gpu are 'deliberately absent', but because their na | Move the vast-mock namespace/quota/RBAC and the fake-gpu-plugin SA/ClusterRole out of base into the  |

---
## 3. DGX Day-0 runbook 骨架(硬件维给出的 14 步,R=现在可预建/排练)

Day-0 runbook skeleton (R = pre-buildable/rehearsable on this EC2 now): 1) rack/power/BMC access + firmware inventory vs written baseline (R: checklist/script); 2) DGX OS validation, NVMe layout, nvidia-smi + field diag per node (R: scripts only); 3) fabric bring-up — cabling plan, switch/subnet-manager config, ibstat link-rate checks (blocked on fabric DECISION; R: check scripts); 4) admin/control-plane host up (blocked on placement DECISION), containerd + kubeadm init from pre-written ClusterConfiguration reusing POD_CIDR/SERVICE_CIDR, vendored CNI applied, 4 nodes joined (R: every manifest and script); after CNI: RE-VERIFY kubelet probes pass the platform-internal-ingress policy (kindnetd verification does not transfer — see networkpolicies.yaml); 5) private registry up, digest-pinned image set mirrored and verified (R: fully rehearsable via kind+local-registry pattern); 6) `make dgx-deploy` (EXISTS since 2026-08-26: dgx-render → dgx-platform → dgx-code) — completed dgx overlay + monitoring + storage classes are BUILT; vendored Volcano still to add (R: rehearsable as a dgx-sim overlay on the kind cluster); 7) GPU Operator with the decided driver flags → nvidia.com/gpu=8 per node, DCGM scraping and alerts live (R: values + helm template render); 8) Network Operator + RDMA resources → NCCL 2-node all-reduce and single-node NVLink bandwidth meet the contractually written thresholds (R: vcjob manifests + thresholds); 9) NVMe storage classes PRE-BUILT (arise-shared/arise-longterm on local-path@digest at /raid; per-node step: verify RAID mount then `sudo install -d /raid/arise/volumes && sudo touch /raid/arise/volumes/.raid-verified` — provisioning fails closed without it); fio baseline recorded, real sanitize path timed (D2 still owns the durability promise); 10) `onboard-node.sh gpu dgx01..04` — the one step already rehearsed on every matrix run (NODE-01); 11) verify-dgx.sh completion gate + KUBE_CONTEXT-parameterized smoke matrix against the real cluster (R: build and dry-run now); 12) TLS-terminated ingress + seeded credentials replaced before any external exposure (credentials: `make dgx-gateway-secret` BEFORE `make dgx-deploy` — the gateway will not start without it; at cutover flip GW_COOKIE_SECURE and GW_TRUST_PROXY to "true" together in platform/overlays/dgx/gateway.yaml, never one without the other); 13) HW-01..15 acceptance matrix executed and evidenced; 14) end-to-end DIRECT-reservation dry run before the first paying customer. Steps 4-8 and 11 are the pre-build backlog; steps 5, 6 and 11 can be rehearsed to completion on this machine before the hardware ships.

---
## 4. EC2-NOW 执行序(不等任何决策就能开工的部分)

| 工作流 | 内容 | 解锁什么 |
|---|---|---|
| WS1 | ~~**dgx overlay 补全工作负载**~~ **〔DONE 2026-08-26〕** + registry 化镜像交付(本地 registry 在 kind 里排练),SPA 交付去 docker-cp 化 | 几乎一切;Day-0 步骤 5/6 可排练到位 |
| WS2 | ~~**gateway 公网化改造**~~ **〔DONE 2026-08-27〕**:secrets fail-closed(无密码即拒启)、登录限速/锁定(IP 严格/用户名宽松)、服务端超时与连接数上限、无状态签名会话(重启与多副本可用)、CSRF/cookie/HSTS、访问日志 | 公网维 5 个 P0 已关;剩 TLS 边缘(D4)与用户表持久化(D3) |
| WS3 | ~~**租户入驻自动化**~~ **〔DONE 2026-08-27〕**:`platform/tenants.yaml` 单一真相源 + 标签驱动的围栏与队列绑定 + `onboard-tenant.py` 生成器 + L0 一致性门(§11);platform-internal-ingress 的 fail-open 已修 | 客户产品链前半段;账号在控制台运行时创建,无需改代码 |
| WS4 | **客户工作负载访问通道**〔集群内半段完成 2026-08-27〕:~~SSH 公钥注入~~、~~非 root sshd 开发机~~、~~scp/rsync 数据通道~~、~~可写 home~~;公网接入地址(bastion/LB)等 D4 | 产品存在性 |
| WS5 | **计量骨架**〔完成 2026-08-27〕:~~分配台账控制器~~、~~价格本~~、~~per-tenant 序列~~、~~CSV 发票~~;剩 RentalAgreement 记录与欠费冻结(D6) | 收钱的地基 |
| WS6 | **运维地基**〔大部分完成 2026-08-27〕:~~etcd 快照 CronJob~~(lab 实跑过)、~~receiver 一命令化~~、~~四本事故 runbook~~、~~维护模式进状态机~~(MNT-01)、~~混沌演练~~(CHAOS-01);仍缺:恢复演练本体、离节点备份副本、日志聚合、滚动升级流程 | 可靠性维大半 |
| WS7 | **Day-0 工具包**〔部分完成 2026-08-27〕:~~kubeadm ClusterConfiguration~~、~~verify-dgx 完成门~~、~~测试矩阵 KUBE_CONTEXT 参数化~~;仍缺 registry 引导脚本、GPU/Network Operator values | 到货即用 |

> 顺序建议:WS1 → WS2/WS3 并行 → WS6 → WS5 → WS4/WS7。
> 决策 D1–D8 任何一个落地,立即回填对应工作流。