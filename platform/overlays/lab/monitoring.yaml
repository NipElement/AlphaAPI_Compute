# ============================================================================
# Monitoring — Prometheus + Alertmanager (plan §9.1, §9.2, §9.3)
#
# Scope: this is the OBSERVER, never the source of truth. Plan §3.3 is explicit
# that Prometheus/Grafana "描述实际状态…不成为 source of truth 或自动改 owner".
# Prometheus therefore holds only read verbs, and no alert has automated
# remediation attached: a metric disagreeing with the controller is a signal
# for a human, never a trigger.
#
# STANDARD budget (§9.1): Prometheus 300m/768Mi, Alertmanager 50m/128Mi,
# retention 3d. Loki is NOT deployed on this host (§9.1 small-instance note).
# Alertmanager sinks locally only — Phase A pages nobody.
# ============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: prometheus
  namespace: monitoring
  labels: { project: arise-b300-prelab }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: arise:prometheus
rules:
  # Read-only everywhere. An observer that can mutate is not an observer.
  - apiGroups: [""]
    resources: ["nodes", "nodes/metrics", "services", "endpoints", "pods"]
    verbs: ["get", "list", "watch"]
  - nonResourceURLs: ["/metrics"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: arise:prometheus
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: ClusterRole, name: arise:prometheus }
subjects:
  - { kind: ServiceAccount, name: prometheus, namespace: monitoring }
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: monitoring
  labels: { project: arise-b300-prelab }
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
      external_labels:
        cluster: b300-prelab
    rule_files:
      - /etc/prometheus/rules/*.yml
    alerting:
      alertmanagers:
        - static_configs:
            - targets: ['alertmanager.monitoring.svc:9093']
    scrape_configs:
      - job_name: capacity-controller
        static_configs:
          - targets: ['capacity-controller.platform-system.svc:8080']
      # The allocation ledger's per-tenant series: a CROSS-CHECK on the ledger
      # (chain_ok, open intervals, gpu-seconds), never the billing source.
      - job_name: metering
        static_configs:
          - targets: ['metering.platform-system.svc:8080']
      - job_name: fake-gpu-advertiser
        static_configs:
          - targets: ['fake-gpu-advertiser.platform-system.svc:8080']
      - job_name: kubernetes-nodes
        scrape_interval: 30s
        kubernetes_sd_configs: [{ role: node }]
        scheme: https
        tls_config:
          ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
          insecure_skip_verify: true
        bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
        relabel_configs:
          - action: labelmap
            regex: __meta_kubernetes_node_label_(.+)
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-rules
  namespace: monitoring
  labels: { project: arise-b300-prelab }
data:
  arise.yml: |
    groups:
      # The money path. Ported from the dgx overlay (2026-08-30) minus the two
      # rules that need dgx-only exporters (kube-state-metrics, kubelet volume
      # stats). Here so the lab can PROVE they fire, the way OBS-02 proves
      # OwnerConflict does — an alert nobody has ever seen fire is a wish.
      - name: arise-billing
        rules:
          - alert: MeteringDown
            expr: up{job="metering"} == 0 or absent(up{job="metering"})
            for: 2m
            labels: { severity: critical, runbook: incident-metering }
            annotations:
              summary: "the allocation meter is not being scraped"
              description: "Usage accrued while the meter was down is NOT in the ledger."
          - alert: LedgerChainBroken
            expr: arise_metering_ledger_chain_ok == 0
            for: 0m
            labels: { severity: critical, runbook: incident-metering }
            annotations:
              summary: "allocation ledger hash chain is BROKEN"
              description: "Do not invoice from this ledger; compare the head against the anchor."
          - alert: LedgerShrank
            expr: arise_metering_ledger_records < max_over_time(arise_metering_ledger_records[24h])
            for: 5m
            labels: { severity: critical, runbook: incident-metering }
            annotations:
              summary: "the append-only ledger got SHORTER ({{ $value }} records)"
              description: "Records were deleted or the PVC replaced."
          - alert: MeteringPollErrors
            expr: increase(arise_metering_poll_errors_total[15m]) > 3
            for: 0m
            labels: { severity: warning, runbook: incident-metering }
            annotations:
              summary: "the meter is failing to poll or to write"
              description: "A full or read-only ledger PVC drops intervals for short-lived pods."
      - name: arise-ownership
        rules:
          # P0 — the single-owner invariant. Two owner series at 1 means
          # Kubernetes and VAST could both schedule the same GPU; zero means
          # the controller lost a node. Both are P0.
          - alert: OwnerConflict
            expr: sum by (node) (arise_node_owner) != 1
            for: 1m
            labels: { severity: P0 }
            annotations:
              summary: "node {{ $labels.node }} does not have exactly one owner"
              description: >-
                sum(arise_node_owner)={{ $value }} for 1m. Quarantine the node;
                stop all scheduling and listing for it.

          # P0 — contract protection. ARISE-owned while contracts are live
          # would mean a paying rental was reclaimed underneath the customer.
          - alert: ContractReclaimAttempt
            expr: (arise_active_contracts > 0) and on(node) (arise_node_owner{owner="ARISE"} == 1)
            for: 30s
            labels: { severity: P0 }
            annotations:
              summary: "{{ $labels.node }} is ARISE-owned while contracts are active"
              description: >-
                An active rental must run to its end; unlist is not reclaim.
                Freeze the controller and confirm with the approver.

      - name: arise-transitions
        rules:
          # P1 — the controller's own escalate-to-a-human state. Quarantine is
          # what it does when it refuses to proceed: a failed sanitization, a
          # non-zero GPU allocation on a node about to change hands, an
          # eviction that never completed, a contract on a node the
          # marketplace should not own. Until 2026-08-31 NOTHING watched it:
          # the node sums to exactly one owner, so OwnerConflict stays quiet,
          # and a B300 sat out of the fleet earning nothing with no page.
          - alert: NodeQuarantined
            # Read from the CR's phase, NOT from arise_node_owner: the owner
            # series comes from a node LABEL, so a NodeNotFound quarantine —
            # the label is gone, that is the whole reason — leaves it stale at
            # the last owner. Measured 2026-08-31: dgx03 QUARANTINED while
            # arise_node_owner{owner="ARISE"} still read 1.
            expr: arise_node_phase{phase="QUARANTINED"} == 1
            for: 1m
            labels: { severity: P1 }
            annotations:
              summary: "{{ $labels.node }} is QUARANTINED — out of the fleet"
              description: >-
                The controller stopped and is waiting for a human. Read
                status.conditions on the NodeOwnership for the reason before
                touching anything; do NOT clear the owner label to "fix" it.
                runbooks/incident-quarantine-recovery.md

          # P1 — the deadline's backstop. The drain deadline quarantines a
          # stuck handover at DRAIN_TIMEOUT_SECONDS (600s); this fires if a
          # node is mid-transition well past that, which means the hang is in
          # a phase no deadline covers, or the deadline logic itself broke.
          # An independent clock, read from the metric, not from the code
          # that is supposed to enforce it.
          - alert: NodeTransitionStuck
            expr: arise_node_transition_seconds > 1800
            for: 1m
            labels: { severity: P1 }
            annotations:
              summary: "{{ $labels.node }} stuck in {{ $labels.phase }} for {{ $value | humanizeDuration }}"
              description: >-
                A handover that cannot finish holds a machine cordoned out of
                the sellable fleet. The drain deadline should have quarantined
                it already; that it did not is itself the finding.

          - alert: ControllerDown
            expr: up{job="capacity-controller"} == 0
            for: 2m
            labels: { severity: P1 }
            annotations:
              summary: "capacity-controller is not scrapeable"
              description: >-
                Ownership is unobservable while this is down. Do NOT make
                manual owner changes to compensate.

          - alert: ControllerReconcileErrors
            expr: sum(rate(arise_reconcile_errors_total[5m])) > 0
            for: 5m
            labels: { severity: P1 }
            annotations:
              summary: "capacity-controller reconcile errors for 5m"

      - name: arise-capacity
        rules:
          - alert: FakeGPUHealthLoss
            expr: sum(arise_fake_gpu_healthy) < 32
            for: 2m
            labels: { severity: P1, simulated: "true" }
            annotations:
              summary: "simulated GPU health degraded: {{ $value }}/32"
              description: >-
                SIMULATED. On real hardware this maps to DCGM/Xid and is HW-03,
                not a control-plane result.

          # NOTE (2026-08-17): a HostDiskLow alert used to sit here, on
          # node_filesystem_avail_bytes. It was REMOVED because this cluster
          # runs no node-exporter, so that series never existed and the rule
          # could never fire — an alert that cannot fire is worse than no
          # alert, because it looks like disk is being watched when it is not.
          #
          # The real WAIVER-2026-08-11-001 W-3 compensating control is
          # scripts/guard.sh: it reads `df` directly and HARD-STOPS every
          # mutating make target below 40 GiB free (STOP_FREE_GIB=40). That
          # runs on the host, needs no exporter, and is exercised on every
          # deploy/test. If host-level alerting is ever wanted here, deploy
          # node-exporter first and restore a rule against a series that
          # actually exists.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-config
  namespace: monitoring
  labels: { project: arise-b300-prelab }
data:
  alertmanager.yml: |
    global:
      resolve_timeout: 5m
    route:
      receiver: local-sink
      group_by: ['alertname', 'node']
      group_wait: 10s
      group_interval: 1m
      repeat_interval: 1h
      routes:
        - matchers: [ 'severity="P0"' ]
          receiver: local-sink
          group_wait: 0s        # P0 must not wait for grouping
          repeat_interval: 5m
    receivers:
      # Local only. Phase A sends nothing outside this cluster (§9.1).
      - name: local-sink
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: monitoring
  labels: { app.kubernetes.io/name: prometheus, project: arise-b300-prelab }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: prometheus } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: prometheus, project: arise-b300-prelab }
    spec:
      serviceAccountName: prometheus
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: prometheus
          image: prom/prometheus@sha256:508729e0e2d18e11fd742a5a5ca70e557b940a93948c3c95fd0123a6fd538b69
          args:
            - --config.file=/etc/prometheus/prometheus.yml
            - --storage.tsdb.path=/prometheus
            - --storage.tsdb.retention.time=3d
            - --web.listen-address=0.0.0.0:9090
            - --web.enable-lifecycle
          ports: [{ name: http, containerPort: 9090 }]
          resources:
            requests: { cpu: 300m, memory: 768Mi }
            limits:   { cpu: "1",  memory: 1200Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: config, mountPath: /etc/prometheus }
            - { name: rules,  mountPath: /etc/prometheus/rules }
            - { name: data,   mountPath: /prometheus }
          readinessProbe:
            httpGet: { path: /-/ready, port: http }
            initialDelaySeconds: 15
            failureThreshold: 30
      volumes:
        - { name: config, configMap: { name: prometheus-config } }
        - { name: rules,  configMap: { name: prometheus-rules } }
        - { name: data,   emptyDir: {} }
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: monitoring
  labels: { app.kubernetes.io/name: prometheus }
spec:
  type: ClusterIP        # never NodePort; remote access is port-forward only
  selector: { app.kubernetes.io/name: prometheus }
  ports: [{ name: http, port: 9090, targetPort: http }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alertmanager
  namespace: monitoring
  labels: { app.kubernetes.io/name: alertmanager, project: arise-b300-prelab }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: alertmanager } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: alertmanager, project: arise-b300-prelab }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: alertmanager
          image: prom/alertmanager@sha256:9e082985f56f4c8c9f724e18f2288c6708f472e56a5286b8863d080434ea065d
          args:
            - --config.file=/etc/alertmanager/alertmanager.yml
            - --storage.path=/alertmanager
            - --web.listen-address=0.0.0.0:9093
          ports: [{ name: http, containerPort: 9093 }]
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits:   { cpu: 200m, memory: 256Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: config, mountPath: /etc/alertmanager }
            - { name: data,   mountPath: /alertmanager }
          readinessProbe:
            httpGet: { path: /-/ready, port: http }
            initialDelaySeconds: 10
      volumes:
        - { name: config, configMap: { name: alertmanager-config } }
        - { name: data,   emptyDir: {} }
---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: monitoring
  labels: { app.kubernetes.io/name: alertmanager }
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: alertmanager }
  ports: [{ name: http, port: 9093, targetPort: http }]
